# Default printers config
# Put each printer in other lines, with following pattern. Each value of printer must be separated with tab character.
# Name	Address	ModelID
Test_printer	https://localhost/	T_002